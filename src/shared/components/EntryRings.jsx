import './EntryRings.css'
import { RingsIcon } from '@shared/components/ui/icon/RingsIcon'

export default function EntryRings({ offsets }) {
  return <RingsIcon offsets={offsets} className="be-entry-rings" />
}
